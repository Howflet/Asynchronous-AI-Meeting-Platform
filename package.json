{
  "name": "a2mp-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n BACKEND,NEXTJS -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix nextjs-frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix nextjs-frontend",
    "format": "prettier -w \"**/*.{ts,tsx,js,json,css,md}\""
  },
  "devDependencies": {
    "@types/pg": "^8.15.6",
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "cloudflared": "^0.7.1",
    "pg": "^8.16.3",
    "socket.io-client": "^4.8.1"
  }
}
